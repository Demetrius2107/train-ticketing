<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.trainticketing.business.mapper.DailyTrainSeatMapper">
  <resultMap id="BaseResultMap" type="com.trainticketing.business.domain.DailyTrainSeat">
    <id column="id" jdbcType="BIGINT" property="id" />
    <result column="daily_train_id" jdbcType="BIGINT" property="dailyTrainId" />
    <result column="train_seat_id" jdbcType="BIGINT" property="trainSeatId" />
    <result column="carriage_id" jdbcType="BIGINT" property="carriageId" />
    <result column="seat_index" jdbcType="INTEGER" property="seatIndex" />
    <result column="seat_label" jdbcType="CHAR" property="seatLabel" />
    <result column="seat_type" jdbcType="CHAR" property="seatType" />
    <result column="sale_status" jdbcType="CHAR" property="saleStatus" />
    <result column="create_time" jdbcType="TIMESTAMP" property="createTime" />
    <result column="update_time" jdbcType="TIMESTAMP" property="updateTime" />
  </resultMap>
  <sql id="Base_Column_List">
    id, daily_train_id, train_seat_id, carriage_id, seat_index, seat_label, seat_type, sale_status, create_time, update_time
  </sql>
  <!-- 带 s 别名的字段列表：JOIN train_order / train_order_item 的查询必须用它，否则 id 等字段在多表间歧义 -->
  <sql id="Alias_Column_List">
    s.id, s.daily_train_id, s.train_seat_id, s.carriage_id, s.seat_index, s.seat_label, s.seat_type, s.sale_status, s.create_time, s.update_time
  </sql>
  <insert id="insertBatch" parameterType="java.util.List">
    insert into daily_train_seat (id, daily_train_id, train_seat_id, carriage_id, seat_index, seat_label, seat_type, sale_status, create_time, update_time)
    values
    <foreach collection="list" item="item" separator=",">
      (#{item.id,jdbcType=BIGINT}, #{item.dailyTrainId,jdbcType=BIGINT}, #{item.trainSeatId,jdbcType=BIGINT},
      #{item.carriageId,jdbcType=BIGINT}, #{item.seatIndex,jdbcType=INTEGER}, #{item.seatLabel,jdbcType=CHAR},
      #{item.seatType,jdbcType=CHAR}, #{item.saleStatus,jdbcType=CHAR},
      #{item.createTime,jdbcType=TIMESTAMP}, #{item.updateTime,jdbcType=TIMESTAMP})
    </foreach>
  </insert>
  <select id="selectCountByDailyTrainId" resultType="int">
    select count(1)
    from daily_train_seat
    where daily_train_id = #{dailyTrainId,jdbcType=BIGINT}
  </select>
  <select id="selectByDailyTrainId" resultMap="BaseResultMap">
    select
    <include refid="Base_Column_List" />
    from daily_train_seat
    where daily_train_id = #{dailyTrainId,jdbcType=BIGINT}
    order by carriage_id, seat_index, seat_label
  </select>
  <!-- 可售判断统一用 NOT EXISTS（不能用 LEFT JOIN order 再判 oi.id is null：
       座位×订单会行数膨胀导致 count 虚高；且订单状态过滤会漏算待支付占用。
       占用事实只看 train_order_item——取消/退票/超时关单均删除明细，天然释放） -->
  <select id="selectRemainingByInterval" resultType="com.trainticketing.business.resp.SeatRemainingResp">
    select s.seat_type as seatType,
           sum(case when not exists (
             select 1 from train_order_item oi
             where oi.daily_train_seat_id = s.id
               and oi.depart_index &lt;= #{arriveIndex,jdbcType=INTEGER}
               and oi.arrive_index &gt;= #{departIndex,jdbcType=INTEGER}
           ) then 1 else 0 end) as remainingCount
    from daily_train_seat s
    where s.daily_train_id = #{dailyTrainId,jdbcType=BIGINT}
      and s.sale_status = '0'
    group by s.seat_type
    order by s.seat_type
  </select>
  <select id="selectAvailableByInterval" resultMap="BaseResultMap">
    select
    <include refid="Alias_Column_List" />
    from daily_train_seat s
    where s.daily_train_id = #{dailyTrainId,jdbcType=BIGINT}
      and s.sale_status = '0'
      and s.seat_type = #{seatType,jdbcType=CHAR}
      and not exists (
        select 1 from train_order_item oi
        where oi.daily_train_seat_id = s.id
          and oi.depart_index &lt;= #{arriveIndex,jdbcType=INTEGER}
          and oi.arrive_index &gt;= #{departIndex,jdbcType=INTEGER}
      )
    order by s.carriage_id, s.seat_index, s.seat_label
    limit #{limit,jdbcType=INTEGER}
  </select>

  <!-- 下单选座（FOR UPDATE 行锁兜底，须在事务内调用）：语义同 selectAvailableByInterval -->
  <select id="selectAvailableForUpdate" resultMap="BaseResultMap">
    select
    <include refid="Alias_Column_List" />
    from daily_train_seat s
    where s.daily_train_id = #{dailyTrainId,jdbcType=BIGINT}
      and s.sale_status = '0'
      and s.seat_type = #{seatType,jdbcType=CHAR}
      and not exists (
        select 1 from train_order_item oi
        where oi.daily_train_seat_id = s.id
          and oi.depart_index &lt;= #{arriveIndex,jdbcType=INTEGER}
          and oi.arrive_index &gt;= #{departIndex,jdbcType=INTEGER}
      )
    order by s.carriage_id, s.seat_index, s.seat_label
    limit #{limit,jdbcType=INTEGER}
    for update
  </select>

  <!-- 选座策略用：全量可售座位加行锁，不限数量，供内存贪心选座 -->
  <select id="selectAllAvailableForUpdate" resultMap="BaseResultMap">
    select
    <include refid="Alias_Column_List" />
    from daily_train_seat s
    where s.daily_train_id = #{dailyTrainId,jdbcType=BIGINT}
      and s.sale_status = '0'
      and s.seat_type = #{seatType,jdbcType=CHAR}
      and not exists (
        select 1 from train_order_item oi
        where oi.daily_train_seat_id = s.id
          and oi.depart_index &lt;= #{arriveIndex,jdbcType=INTEGER}
          and oi.arrive_index &gt;= #{departIndex,jdbcType=INTEGER}
      )
    order by s.carriage_id, s.seat_index, s.seat_label
    for update
  </select>
  <!-- 余票查询按类型逐个走缓存，此查询提供该排班的座位类型清单 -->
  <select id="selectSeatTypes" resultType="java.lang.String">
    select distinct seat_type
    from daily_train_seat
    where daily_train_id = #{dailyTrainId,jdbcType=BIGINT}
    order by seat_type
  </select>
</mapper>
